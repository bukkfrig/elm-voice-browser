module Vxml.XmlDataTypes exposing (CData, Duration, Event, Expression, NMTOKEN, NMTOKENS, RestrictedVariableName, URI)

{- This module is just a placeholder for these type aliases until these types are properly defined for use in the Vxml package. -}


type alias CData =
    String


type alias Duration =
    String


type alias NMTOKEN =
    String


type alias NMTOKENS =
    String


type alias Expression =
    String


type alias URI =
    String


type alias RestrictedVariableName =
    NMTOKEN


type alias Event =
    String
